flask==2.0.3
requests==2.21.0
pytest==7.4.1
pytest-flask==1.3.0  # Replace with the version that is compatible with your project
werkzeug==2.0.2  # Replace with the version that is compatible with Flask 2.x
